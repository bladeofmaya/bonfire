<%= render Rooms::SharedListItemComponent.new(
      room: room,
      position: room.position,
      unread: local_assigns.fetch(:unread, false),
      reorderable: local_assigns.fetch(:administrator, Current.user&.administrator?)
    ) %>
