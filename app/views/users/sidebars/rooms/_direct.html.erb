<% cache [ membership, membership.room, *participants ] do %>
  <%= render Rooms::DirectListItemComponent.new(
        room: membership.room,
        participants: participants,
        unread: membership.unread?,
        sort_timestamp: membership.room.updated_at
      ) %>
<% end %>
