<%= helpers.link_to_room room,
      id: helpers.dom_id(room, :list), data: { sorted_list_name: room.name },
      style: "--column-gap: 0.5em", class: [ "align-center gap room btn txt-nowrap", "unread": unread? ] do %>
  <span class="overflow-ellipsis"><%= room.name %></span>
<% end %>
